import styled from '@emotion/styled';
import Box from '@mui/material/Box/Box';

export const InfoPanelCont = styled(Box)(() => ({
  display: 'flex',
  gap: 15,
  padding: 15,
  alignItems: 'center',
  width: '100%',
  borderRadius: 18,
}));
